node_modules/
dist/
dist-ssr/
*.local
.DS_Store
.vite/

# Original SimTower game files and anything extracted from them. The art in
# SIMTOWER.EXE is © Maxis / OPeNBooK / Yoot Saito and cannot be redistributed.
# simtowerweb reads it from a copy the player supplies, in the player's own
# browser, and nothing it produces is ever committed or deployed — but the
# files land in working copies, so refuse them at any depth rather than trust
# `git add -A`. See simtowerweb/README.md.
*.EXE
*.exe
*.EX_
*.ex_
assets-private/
