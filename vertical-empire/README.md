# Vertical Empire

A portrait-first tower builder in the SimTower idiom. This is a **look-and-feel
spike**, not a game: there is no simulation, no economy and no failure state. It
exists to answer one question before any of that gets built —

> Does a SimTower-shaped tower read on a phone held upright?

The answer, from the screenshots in [`screenshots/`](screenshots), is yes, at one
art pixel per CSS pixel. See [What the spike found](#what-the-spike-found).

```bash
npm install
npm run dev
```

## Where the art comes from

SimTower's own bitmaps live inside `SIMTOWER.EXE`. They are © Maxis / OPeNBooK /
Yoot Saito, EA-owned, and were never released as freeware, so **this repository
contains none of them and never will** — committing them here or syncing them to
a public bucket would be redistribution.

What it does instead is what OpenSkyscraper does, moved into the browser:

- **Placeholder art** ships with the site. It is generated by
  [`src/assets/fallback.ts`](src/assets/fallback.ts), drawn to the original's
  8×36 grid, and it is ours.
- **Your own copy** can be pointed at the page with *Use my SimTower art*. The
  file is read in the browser, the sprites are cut out in the browser, and the
  result is cached in IndexedDB on your device. Nothing is uploaded. Nothing is
  written to this repository.

To look at what is inside a copy from the command line:

```bash
npm run extract -- /path/to/SIMTOWER.EXE
```

That prints an inventory of every resource and writes PNGs to `assets-private/`,
which is gitignored. A compressed `SIMTOWER.EX_` has to be expanded first
(`expand.exe`, or `msexpand`); the loader only reads the uncompressed form.

> **The resource map is unverified.** `src/assets/slice.ts` was written against
> published format documentation, not against a copy of the game — there was no
> copy to hand while building this. The container parsing is tested and correct;
> the *IDs* may not be. If a sprite comes out wrong, the inventory tells you
> which IDs actually exist and the PNGs show what is in them. Correcting the
> table should be minutes of work, and it is the first thing to do with a real
> copy.

## What the spike found

**One art pixel per CSS pixel is the right scale on a phone.** At 2× a 390px
screen shows 24 segments — barely two offices side by side — and the tower stops
reading as a tower. At 1× you get 49 segments and 23 floors, close to what the
original showed on a 640×480 screen. The app picks 1× below 560px and 2× above.

**The lot wants to be narrower than the original's.** 375 segments is a map you
never see the edges of on a phone. [`src/world/grid.ts`](src/world/grid.ts) caps
it at 96 — four screens across at 1× — and that is still the number most worth
arguing about. Change `LOT_SEGMENTS`, reload, look.

**The palette does most of the work.** Day and night are one lookup table
sliding into another across the whole screen, exactly as the original did it,
which is why nothing in the pipeline flattens to RGB until the last step.

## How it is put together

| | |
| --- | --- |
| [`src/assets/ne.ts`](src/assets/ne.ts) | Walks the resource table of a 16-bit Windows executable. |
| [`src/assets/dib.ts`](src/assets/dib.ts) | Headerless DIBs, and the 8×36 cell strips that have no header at all. |
| [`src/assets/palette.ts`](src/assets/palette.ts) | Colour tables, the 197–203 rotation, day/night mixing. |
| [`src/assets/slice.ts`](src/assets/slice.ts) | Which resource is which, and how to cut it up. **Unverified — see above.** |
| [`src/assets/fallback.ts`](src/assets/fallback.ts) | Our own sprites, generated on the same grid. |
| [`src/render/framebuffer.ts`](src/render/framebuffer.ts) | 8-bit indexed buffer, resolved through the palette once a frame. |
| [`src/render/scene.ts`](src/render/scene.ts) | Sky by altitude band, floors, lifts, pedestrians. |
| [`src/world/`](src/world) | The grid, what can be built, and what has been. |

The measurements, all recovered from published documentation and none of them
copyrightable, are what actually carry the resemblance:

| | |
| --- | --- |
| Segment | 8 px wide |
| Floor | 36 px tall |
| World | 375 segments × 100 floors + 9 basements (we use 96 segments) |
| Elevator car | 32×36; express 48×36 |
| Sheet layout | states horizontally, variants vertically |
| Animation | palette rotation on indices 197–203; eleven sky tables across the day |

## Controls

Drag to pan, pinch or double-tap to zoom, tap a tool then tap the grid to build,
long-press to clear. `?hour=22` pins the clock, which is how the night
screenshots are taken without waiting for night.

## Checks

```bash
npm test        # 40 tests: extractor, grid, renderer, camera
npm run build   # tsc --noEmit, then vite build
npm run verify  # Playwright screenshots at three viewports (run `npm run preview` first)
```

The extractor is tested against synthetic executables built in
[`tests/fixtures.ts`](tests/fixtures.ts) — a real NE container, real DIBs, real
cell strips, and not one byte of anybody's copyrighted art.

## What is not here

No simulation of any kind: no tenants, no lift scheduling, no money, no star
rating, no save. Those are the next question, and they are worth answering only
if the look holds up.
